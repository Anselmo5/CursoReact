import React from 'react'

export const Fragmentsestrute = () => {
    // o fragment nos tras uma outra posibilidade de estruturação do nosso jsx(html), onde comumente temos sempre uma dive que vai iniciar o nosso codigo,
    // e isso pode trazendo uma certa complexidade para algo que e simples, nos obricando a utilizar muitas regras de css para colocarmos do nosso gosto,
    // mas com o fragment criamos uma tag nula onde ela sera renderizada no elemento pai dentro do (app) dornando assim o codigo mais simples.

  return (
    <>
        <h1>Fragmento nulo</h1>
        <h2>Oiii</h2>
        
    </>
  )
}
